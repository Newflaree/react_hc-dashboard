import { createTheme } from '@mui/material/styles';

export const appTheme = createTheme({
  palette:{
    mode: 'light',
    primary: {
      main: '#1D3B86'
    },
    primary: {
      main: '#1D3B86'
    },
    error: {
      main: '#D32F2F'
    }
  }
});
